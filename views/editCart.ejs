<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Foodiess</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Foodiess</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addOrder">Add new order</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Order</h2></div>
        <br>
        <div class="text-center">
          <% if (product) { %> 
            <form action="/editOrder/<%= order.idorder %>" method="POST" enctype="multipart/form-data">
              <label for="name">Food Name:</label><br>
                <input type="text" id="name" name="name" value = "<%= order.name %>" required><br><br>
    
                <label for="totalPrice">Food Price:</label><br>
                <input type="number" id="price" name="price" min="0" step="0.01" value = <%= order.totalPrice %> required><br><br>

                <label for="status">Status:</label><br>
                <input type="text" id="status" name="status" min="0" step="1" value = <%= order.status %> required><br><br>
                
                <button type="submit">Update Order</button>
            </form>
          <% } else { %>
              <p>No Order found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
